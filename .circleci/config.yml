version: 2
jobs:
  build:
      docker:
        - image: circleci/node:8.2.0
      steps:
        - checkout
        - run: echo "A first hello"

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master